# A 股基础策略筛选器 V0.1 项目日志

## 开发方式

采用教学施工模式：每次只推进一个小步骤，执行前说明目的、文件和命令，执行后说明结果和失败原因；关键代码用中文解释。

## 已完成

- 按用户要求建立 PROJECT_PLAN.md：整体结构、版本边界、阶段验收、类甘特时间安排和教学施工流程。后续版本为规划建议，尚未开始开发。
- 创建 app.py：Streamlit 页面标题为“A股基础策略筛选器 V0.1”。
- 使用 AKShare 的 stock_zh_a_spot_em() 获取东方财富沪深京 A 股行情，无需用户 Token 或付费 API。
- 选取指定的 11 个字段，只显示接口返回的前 30 条，保留股票代码前导零。
- 提供刷新按钮、请求异常提示、空数据提示、缺失字段提示。
- 创建 requirements.txt，在项目 .venv 中安装 AKShare 1.18.96 和 Streamlit 1.64.0。
- 语法检查通过；使用模拟数据的 Streamlit 页面检查通过：30 行、11 列、代码前导零，以及请求异常和空数据提示。
- 修改表格宽度参数后再次检查通过；授权后检查正常结束；pip check 未发现依赖冲突。

## 当前正在做

- 2026-09-20 16:13：已启动本机 Streamlit 服务 http://127.0.0.1:8501，健康检查返回 ok，并在 Codex 浏览器面板打开。浏览器最终显示效果尚未人工核对。
- 2026-09-20：腾讯真实行情接入成功。用户调整范围为“字段暂不重要，先跑通”，app.py 已切换到 AKShare stock_zh_a_spot_tx()，展示代码、名称、最新价、涨跌幅前 30 条。
- check_live_page.py 直接运行真实数据页面，完整接口请求及 30 行、4 列检查通过，中文名称正常。尚未执行浏览器中的人工视觉验收，未持续启动网页服务。

## 下一步

- 下一小步：启动网页，在浏览器中查看腾讯真实行情并核对展示。字段补齐以后再做，不增加筛选策略、数据库或历史回测。

## 遇到的问题

- 2026-09-20 连接诊断：环境存在 http/https/ftp 代理配置。沙箱外默认请求出现 ProxyError，临时直连出现 ConnectionError，两者底层均为 RemoteDisconnected（未收到 HTTP 响应即断开）。不能断定仅代理故障，也不能据此确定上游限流；需继续定位。
- 默认 python 命令指向 Windows 应用别名，不能正常使用；改用 Codex 内置 Python 创建项目虚拟环境。
- 安装时遇到沙箱临时目录 PermissionError，授权后已成功安装。
- 首次真实请求受到沙箱代理限制；授权后重试仍出现 ProxyError / RemoteDisconnected，连接被关闭，未取得行情。当前证据不足以判断是代理配置还是上游连接限制。
- 页面检查断言通过，但首次运行退出时发生沙箱临时文件清理权限错误。
- 新版 Streamlit 提示 use_container_width 参数废弃，已改为 width="stretch"，最低 Streamlit 版本设为 1.50.0。

## 重要技术决策

- 最新范围优先于早期 11 字段要求：先用腾讯免费行情跑通 4 个基础字段。早期东方财富 11 列检查属于历史记录。
- 新增 check_alternative_sources.py，新浪和腾讯各少量请求均返回 HTTP 200 和行情；新浪 AKShare 标准函数未保留全部估值字段，腾讯接口字段为英文缩写。此阶段不做字段拼接。
- 新增 check_live_page.py：真实数据页面检查，无模拟注入；运行命令为 .\.venv\Scripts\python.exe check_live_page.py。
- 新增 check_connection.py 用于独立诊断：读取当前 AKShare 请求地址与参数，各请求一页；设置连接/读取超时为 5/10 秒；只打印代理是否存在和错误类型，不输出代理凭据。直连仅作用于诊断 Session，不改变系统代理或正式应用。
- 保持单文件页面，不增加复杂架构。
- 接口获取全量行情后再截取前 30 条展示，保留接口原始顺序。
- 页面“市盈率”对应接口“市盈率-动态”，在页面注明口径和其他字段单位。
- 请求失败时显示真实错误，不使用模拟数据冒充实际行情。
- 模拟数据仅用于独立运行的页面检查，不写入应用。
- 不修改 sources/ 下的只读参考文件。

## 运行命令（项目目录内的 PowerShell）

```powershell
.\.venv\Scripts\python.exe -m streamlit run app.py
```

已有依赖安装完成。如需重新安装：

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

接口文档：https://akshare.akfamily.xyz/data/stock/stock.html

## 储存位置调整（2026-09-20）
- 后续代码与文档主目录：D:\chatgpt\a-stock-screener。已复制并校验原项目 7 个代码/文档文件。
- 一般新文档目录：D:\chatgpt\documents。已保存 Codex 全局储存偏好。
- C 盘原件、原虚拟环境和当前运行的网页仍保留；本次未释放旧文件空间，也未改变应用缓存及会话储存位置。
- 新增 start.ps1，在 D 盘运行项目，复用 C 盘原环境。已启动的网页仍使用旧目录；下次重启时使用 D 盘版本。


## 新架构工作栏（2026-09-20）
- 主项目升级为 D:\chatgpt\investment-ai；上文旧路径记录为历史。
- 建立用户指定的目录、产品和研究文档，保留未开发模块占位。
- 行情调用、数据检查和界面分别整理到 src/data 与 src/ui。
- 新增文档工作栏；真实行情入口保留，文档查询不触发网络。
- 原项目保持完整，未迁移虚拟环境、应用缓存或同步参考资料。
- 自动检查结果：待执行。

- 自动检查通过：全部 10 个文档入口、文档不请求网络、行情 30 行/4 列、代码前导零、切页保留结果、刷新失败提示及失败后仍能查询文档。
- 已停止原项目网页服务，并从 D:\chatgpt\investment-ai 启动新版，继续使用端口 8501。

## Codex 边栏归类（2026-09-20）
- 按用户要求建立六个平级分区：投资AI · 01 项目管理、02 产品设计、03 研究资料、04 程序开发、05 策略研究、06 测试验收。
- 投资ai 项目放入项目管理；当前 A 股基础筛选器任务放入程序开发。未新增空任务，其他项目未调整。
- 边栏分区用于任务组织，不等同于磁盘目录或项目内部多级文件树。

## 50股资本候诊台 V0.1 · 2026-09-20
- 最新指令替代此前未完成目标；保留旧Pulse代码和真实8股历史结果，不继续扩展。
- 目标：从理论研究进入可每日使用的研究工作台。
- 初始化50股，10行业各5只，CSV支持软删除和恢复。
- 新增MarketDataService、统一数值字段、批量腾讯快照补充、缓存fallback、行业筛选、搜索、数字排序、管理和详情页。
- 真实运行：50/50报价成功，行情日期2026-09-18；本次18个数值字段全部有值，成交额/成交量均价检查未发现单位异常。
- 字段包括当前价、涨跌幅、涨跌额、涨速、量比、换手率、成交量、成交额、市值、振幅、OHLC、YTD、PE-TTM、PB。
- 诊断只接入初始名单中5股的已有历史体质/行业标签及Pulse结果；日期独立显示。其他45股待诊。地利八诊、舆情、主证等未接入，不伪造。
- 测试17项通过，包含候诊池增删和修改、代码、标准化、异常隔离、排序、筛选、搜索、缓存、50股完整加载、Pulse映射及页面视图。
- 浏览器首次遇到旧进程模块缓存，重启解决。页面服务在127.0.0.1:8501，项目位于D盘。
- 已知限制：免费快照字段契约可能变更；诊断不是当日实时重算；金额统一按列显示单位；多窗口写入无并发锁；虚拟环境暂仍在C盘。

- 最终自动检查18项通过；新增股票管理UI隔离测试（添加预览、确认、修改、移出），真实名单仍50只。
- 浏览器验收：50股列表实际显示；表头涨跌幅升序从-2.95起；搜索688012仅一条；点击行更新诊断对象；中微公司诊断日期明确为2026-09-18。
- 金额/市值按整列数值范围自动选择万、亿或万亿，保持数值排序。

## 2026-09-21 本地站点连接恢复
- 已完成：确认 8501 无监听进程；从 D 盘项目目录后台启动 Streamlit。健康接口返回 HTTP 200 / ok，页面 AppTest 异常数为 0。
- 原因：检查时站点服务未运行；此前退出原因尚未确定。
- 当前正在做：恢复本地页面访问。
- 下一步：继续高成长跃迁观察池及共用资金流字段任务。
- 重要技术决策：日志保存在 D:\chatgpt\investment-ai\reports；本次没有改动行情或诊断逻辑。

## 2026-09-21 共用资金流字段与成长20股
- 已完成：今日净流入列置于涨跌幅后；数值升降序、正负色、万/亿格式、缺失--；资金流来源、更新时间、状态、请求时间在页面和详情可查。
- 已完成：20只用户指定成长股导入，复用 MarketDataService；导航、搜索、阶段筛选、详情和下一验证点。所有成长阶段及七维诊断待诊，未伪造研究结论。
- 实测：50/50和20/20有报价，交易日期均为2026-09-18；08:46:58+08:00资金流请求失败，ProxyError；净流入及主力净流入有效值均为0只（数值存空，不填0）。
- 当前状态：页面与字段完成；可靠的今日净流入数据接入仍未完成。
- 遇到问题：AKShare东方财富排名和个股资金流接口均代理连接失败；个股接口仅主力日资金流，不能证明全口径净流入或盘中更新时间。
- 技术决策：主力字段独立存储，不映射为全口径net_inflow_today；不同源成交额不计算比例，UNKNOWN；隔日今日字段清空；请求限时25秒，首只失败熔断；UI不直接访问AKShare。
- 验证：python -m unittest discover -s tests -q，22项通过；实际刷新结果见reports/fund_flow_acceptance.json。
- 下一步：核实可达且有明确口径与时间戳的资金流来源；历史累计暂不实现。

## 2026-09-21 成长池附件复核
- 已完成：逐项核对附件20只代码和名称全部一致；修复成长池诊断视图仍展示普通脉象列的问题，改为成长阶段、证据、下一验证点等专属字段。
- 验证：成长诊断视图20行，无页面异常，20只阶段均待诊；网站健康检查200。
- 下一步：建立可追溯的诊断证据与复诊规则；定期脉诊与自动移出尚未实现、未启用。
- 决策：后续考虑定时取数、规则检查、历史对比、异常复核、保留移出记录；当前不创建定时任务。

## 2026-09-21 手机与私有发布准备
- 已完成：手机简洁布局、顶部观察池切换、下方诊断选择、窄屏样式；新增cloud_app.py只读云端入口；部署说明和依赖补充；初始化本地Git仓库。
- 验证：23项测试通过；尚未完成手机真机和云端网络验证。
- 当前正在做：私有GitHub与Streamlit部署。
- 阻塞：缺少可用GitHub认证，浏览器登录处于两步验证，等待用户完成；远程仓库和公网网站未创建。
- 决策：用户确认仅自己使用。私有仓库和私有应用分别配置；只读不替代认证；不上传缓存、日志、环境或密钥。
- 下一步：完成登录后上传、部署、验证未登录访问被拒绝及手机端行情刷新。
## 2026-09-21 本地网站自动启动修复
- 已完成：恢复 127.0.0.1:8501，并新增 `start-background.ps1` 与 `open-app.bat`。
- 已完成：在当前用户 Windows 启动目录创建“投资AI候诊台”快捷方式，登录 Windows 后自动启动 Streamlit。
- 原因：电脑重启会结束 Streamlit 进程；原项目只有手动启动脚本，没有登录自启动设置。
- 验证：脚本语法错误数 0；重复运行不新增进程；停止现有服务后可重新拉起，健康检查返回 HTTP 200 / ok；项目 23 项测试通过。
- 注意：127.0.0.1 仅供这台电脑本机访问；公网手机访问仍需完成云端部署。
## 2026-09-21 今日净流入缺失诊断
- 实测缓存70只：行情日期均为2026-09-21；`net_inflow_today`和`main_net_inflow_today`有效值均为0；资金流日期全部为空；错误均为`ProxyError`。
- 直接绕过系统代理访问东方财富资金流源站仍被远端断开，因此本次缺失的直接原因是资金流源站不可达，不是盘后隐藏。
- 发现口径问题：worker只接收“主力净流入-净额”到`main_net_inflow_today`，页面列使用`net_inflow_today`，即使接口恢复也不会把主力口径冒充通用净流入。
- 发现日期规则问题：当前“今日”按电脑自然日判断，周末和节假日会清空上一交易日资金流；应改为与行情`trade_date`对齐并明确显示“交易日资金流”。
- 下一步：用户确认展示口径后，接入可达来源，并将日期判断从自然日调整为行情交易日。
## 2026-09-21 A02｜盘中强势延续诊断 V0.1
- 已完成：新增“今日脉动”入口；50股和成长20股去重后共70只（当前重叠0只）。
- 已完成：配置化阈值、未复权日线基线、腾讯沪深300快照、手动刷新并保存5分钟点时快照、完整参数详情。
- 已完成：P0中20项可计算；P1序列公式与延续/六脉规则；P2拉升/回调量能公式；脉变与后验字段预留。
- 实测：70/70日线基线成功；70/70价格、MA、VWAP和指数数据成功；第一份快照保存至data/intraday_snapshots/2026-09-21.csv。
- 当前缺失：正式行业收益和行业超额、日/分钟净流入、同时间历史成交对比、Level-2。一个时点不足以计算回撤、持续性、突破保持和脉变。
- 数据决策：不使用后复权日线与当前价混算；不使用观察池均值冒充行业指数；不使用日累计资金流冒充5分钟流；不把缺失填0。
- 防未来：日线只取trade_date之前，再加入当前快照；历史状态直接保存，不用未来快照重算过去状态。
- 验证：29项自动检查通过；原50股和成长20股页面继续通过。
- 下一步：交易时段积累至少4个不同时点，再验证P1状态；可靠数据源接通前保持缺失。
